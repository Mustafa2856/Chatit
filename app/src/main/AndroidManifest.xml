<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.chatit">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.Chatit.NoActionBar">
        <service
                android:name=".ServerConnect"
                android:enabled="true"
                android:exported="true">
        </service>

        <activity
                android:name=".Ask_Name"
                android:label="@string/title_activity_ask__name"
                android:theme="@style/Theme.Chatit.NoActionBar.NoActionBar">
        </activity>
        <activity
                android:name=".Chatlist"
                android:label="@string/title_activity_chatlist"
                android:theme="@style/Theme.Chatit.NoActionBar.NoActionBar">
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>